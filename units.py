

for x1 in range(6):
    for x2 in range(6):
        lower_bound_x1 = x1
        upper_bound_x1 = x1+1
        lower_bound_x2 = x2
        upper_bound_x2 = x2+1
        print(lower_bound_x1, ",", upper_bound_x1, ",",
              lower_bound_x2, ",", upper_bound_x2)
